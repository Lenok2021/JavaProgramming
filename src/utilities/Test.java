package utilities;

import day40_FinalKeyword.ProtectedAccessModifier;

public class Test {
    public static void main(String[] args) {

      //  System.out.println(ProtectedAccessModifier.name1); // default can not be visible outside the package
     //   System.out.println(ProtectedAccessModifier.name2); //we called protected modifier
        //  the  only time when it is visible aoutside the pacjage when this class is a subslasss
    }
}
